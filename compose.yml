services:
  app:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
      - "9229:9229"
    environment:
      - PORT=3000
      - NODE_ENV=development
    command: ["npx", "tsx", "--inspect=0.0.0.0:9229", "src/server.ts"]
